<%= form_with(model: session_controller) do |form| %>
  <% if session_controller.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(session_controller.errors.count, "error") %> prohibited this session_controller from being saved:</h2>

      <ul>
        <% session_controller.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :Session, style: "display: block" %>
    <%= form.text_field :Session %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
